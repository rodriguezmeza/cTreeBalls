# ----- MAKE FILE machine definitions -----
# Mario A. Rodriguez-Meza, Ciudad de Mexico, 5.05.2023
#

MDIR := $(CURDIR)

# Keep precision profiles in separate object directories.  Command-line
# overrides such as `make SINGLEPON=1 cballs` take precedence over this default.
#SINGLEPON ?= 0
SINGLEPON ?= 0

ifeq ($(ADDONSON),1)
ifndef CTREEBALLS_ADDONS_SETTINGS_INCLUDED
CTREEBALLS_ADDONS_SETTINGS_INCLUDED := 1
include $(MACHINES_DIR)/addons/Makefile_addons_settings
endif
endif


BUILD_CLASSLIB := noclasslib
ifeq ($(CLASSLIBON),1)
BUILD_CLASSLIB := classlib
endif

BUILD_OPENMP := noopenmp
ifeq ($(OPENMPMACHINE),1)
BUILD_OPENMP := openmp
endif

BUILD_ADDONS := noaddons
ifeq ($(ADDONSON),1)
BUILD_ADDONS := addons
endif

BUILD_MPI := nompi
ifneq ($(filter 1,$(KDTREEMPION) $(KDTREE2BALLSMPION) $(BALLTREEMPION) $(BALLTREE2BALLSMPION) $(OCTREE2BALLSMPION) $(BALLTREE2BALLSMPI3PCFON) $(OCTREEGGGMPION) $(OCTREEBALLS4MPION) $(LYAFORESTMPION) $(OCTREE3PCF3DMPION) $(OCTREESHEARSPHERE2BALLSMPION) $(KDTREESHEARSPHERE2BALLSMPION) $(BALLTREESHEARSPHERE2BALLSMPION)),)
BUILD_MPI := mpi
endif

BUILD_LYA := nolya
ifneq ($(filter 1,$(LYAFORESTOMPON) $(LYAFORESTMPION)),)
BUILD_LYA := lya
endif

BUILD_SHEAR := noshear
SHEAR_SPHERE_ENABLED := $(if $(filter 1,$(OCTREESHEARSPHERE2BALLSOMPON) $(KDTREESHEARSPHERE2BALLSOMPON) $(BALLTREESHEARSPHERE2BALLSOMPON)),1,0)
ifeq ($(SHEAR_SPHERE_ENABLED),1)
BUILD_SHEAR := shear-sphere
endif

BUILD_PRECISION := double
ifeq ($(SINGLEPON),1)
BUILD_PRECISION := mixed
endif

WRKDIR = $(MDIR)/build/$(BUILD_ADDONS)-$(BUILD_CLASSLIB)-$(BUILD_OPENMP)-$(BUILD_MPI)-$(BUILD_LYA)-$(BUILD_SHEAR)-$(BUILD_PRECISION)

.base:
	mkdir -p $(WRKDIR)/lib
	touch $(WRKDIR)/.base

vpath %.c $(MDIR)/source:$(MDIR)/general_libs:$(MDIR)/getparam:$(MDIR)/main
vpath %.o $(WRKDIR)
vpath .base $(WRKDIR)


#B LINES TO ADAPT TO YOUR PLATFORM
#
#--------------------------------------------
# Set other local options to this code
# 	INCLUDES :: OPT1
# 	OTHER OPTIONS :: OPT2
# Some of this definitions can be set in a particular machine
#
OPT1 =
#
# This variable is to show settings
#   and set the corresponding definitions to the compiler
OPT2 =
OPT2LIB =
# The one which will be used by CC is CCFLAG. At some point it gets OPT2 definitions...
#
#
#OPT1    +=  -I ./
#OPT1    +=  -I ./general_libs
# Add as many as needed
#
#OPT2   +=  -DOPTIONNAME
# Add as many as needed

#PRECISION = DOUBLEPREC

#
# your C compiler: Comment/uncomment as convenient
#
# GNU c
# default is gcc-15,
# cyballs module is working with python 3.13 and gcc-15
#   check with: 'python -c "import cyballs"'
CC       = gcc
# tested in MacBook Pro chip: Apple M3 Max
#CC       = gcc-12
#CC       = gcc-15
# With icc (Intel):
#CC       = icc
#

#
#E LINES TO ADAPT TO YOUR PLATFORM

#
# Nothing to do below. Almost... may be some compiler options should be changed
#

#
# Your python interpreter.
# In order to use Python 3, you can manually
# substitute python3 to python in the line below, or you can simply
# add a compilation option on the terminal command line:
# "PYTHON=python3 make all" (THanks to Marius Millea for pyhton3
# compatibility)
PYTHON ?= python
# tested in MacBook Pro chip: Apple M3 Max
#PYTHON = python3.12

# your optimization flag
# clang-12: warning: -O4 is equivalent to -O3 [-Wdeprecated]
#OPTFLAG = -O4 -ffast-math #-march=native
#OPTFLAG = -O3 -ffast-math #-march=native
OPTFLAG = -O3 -fno-fast-math #-march=native
#OPTFLAG = -Ofast -ffast-math #-march=native
#OPTFLAG = -fast

# your openmp flag (comment for compiling without openmp)
OMPFLAG   = -fopenmp
#OMPFLAG   = -mp -mp=nonuma -mp=allcores -g
#OMPFLAG   = -openmp

# all other compilation flags
CCFLAG = -g -fPIC
LDFLAG = -g -fPIC

# Optional focused sanitizer profile, for example:
#   make SANITIZER=address,undefined cballs
ifneq ($(strip $(SANITIZER)),)
SANITIZER_FLAGS = -fsanitize=$(SANITIZER) -fno-omit-frame-pointer
CCFLAG += $(SANITIZER_FLAGS)
LDFLAG += $(SANITIZER_FLAGS)
endif

UNAME_S := $(shell uname -s)
ifeq ($(UNAME_S),Darwin)
PYTHON_MACOSX_DEPLOYMENT_TARGET := $(shell $(PYTHON) -c "import sysconfig; print(sysconfig.get_config_var('MACOSX_DEPLOYMENT_TARGET') or '11.0')")
OPENMP_MACOSX_DEPLOYMENT_TARGET := $(shell runtime="$$($(CC) -print-file-name=libgomp.dylib 2>/dev/null)"; if test -f "$$runtime"; then otool -l "$$runtime" 2>/dev/null | awk '/LC_BUILD_VERSION/{found=1; next} found && /minos/{print $$2; exit}'; fi)
MACOSX_DEPLOYMENT_TARGET ?= $(shell $(PYTHON) -c "import sys; versions=[v for v in sys.argv[1:] if v]; print(max(versions, key=lambda v: tuple(map(int, v.split('.')))))" "$(PYTHON_MACOSX_DEPLOYMENT_TARGET)" "$(OPENMP_MACOSX_DEPLOYMENT_TARGET)")
# Export a value, not a shell command re-evaluated for every recipe.
MACOSX_DEPLOYMENT_TARGET := $(MACOSX_DEPLOYMENT_TARGET)
export MACOSX_DEPLOYMENT_TARGET
MACOS_DEPLOYMENT_FLAG = -mmacosx-version-min=$(MACOSX_DEPLOYMENT_TARGET)
CCFLAG += $(MACOS_DEPLOYMENT_FLAG)
LDFLAG += $(MACOS_DEPLOYMENT_FLAG)
endif

# pass current working directory to the code
CCFLAG += -D__CTREEBALLSDIR__='"$(MDIR)"'

# where to find include files *.h
INCLUDES = -I$(MDIR)/include
HEADERFILES = $(wildcard ./include/*.h)

#INCLUDES += $(OPT1)

#
MLIBS = -lm
#

# your tool for creating static libraries:
AR        = ar rv

# automatically add external programs if needed. First, initialize to blank.
EXTERNAL =

#
ifeq ($(DEFDIMENSION),2)
    DIMCODE = -DTWODIMCODE
    OPT2 += $(DIMCODE)
else
    DIMCODE = -DTHREEDIMCODE
    OPT2 += $(DIMCODE)
endif
#

#
ifeq ($(OPENMPMACHINE),1)
OMPCODE = -DOPENMPCODE
OPT2 += $(OMPCODE)
MLIBS += -lgomp
endif
#

# Optional vector logarithms for batched dual-node pair binning. Linux builds
# auto-enable SLEEF when pkg-config can resolve it; SLEEFON=1 makes absence a
# configuration error, while SLEEFON=0 always keeps the scalar fallback.
SLEEFON ?= auto
SLEEF_PKG ?= sleef
PKG_CONFIG ?= pkg-config
comma := ,
SLEEF_AVAILABLE := $(shell $(PKG_CONFIG) --exists $(SLEEF_PKG) 2>/dev/null && echo 1 || echo 0)
SLEEF_ENABLED := 0
ifeq ($(SLEEFON),1)
ifeq ($(SLEEF_AVAILABLE),0)
$(error SLEEFON=1 but pkg-config cannot resolve $(SLEEF_PKG))
endif
SLEEF_ENABLED := 1
else ifeq ($(SLEEFON),auto)
ifeq ($(UNAME_S),Linux)
SLEEF_ENABLED := $(SLEEF_AVAILABLE)
endif
endif
ifeq ($(SLEEF_ENABLED),1)
SLEEF_CFLAGS := $(shell $(PKG_CONFIG) --cflags $(SLEEF_PKG))
SLEEF_LDFLAGS := $(shell $(PKG_CONFIG) --libs-only-L $(SLEEF_PKG))
SLEEF_LIBS := $(shell $(PKG_CONFIG) --libs-only-l $(SLEEF_PKG))
SLEEF_RPATH := $(patsubst -L%,-Wl$(comma)-rpath$(comma)%,$(SLEEF_LDFLAGS))
OPT2 += -DDUAL_NODE_USE_SLEEF
CCFLAG += $(SLEEF_CFLAGS)
LDFLAG += $(SLEEF_LDFLAGS) $(SLEEF_RPATH)
MLIBS += $(SLEEF_LIBS)
endif
#


# Use float storage for search geometry and double computation/accumulation.
# Set on the command line with `make SINGLEPON=1 ...`.
#
ifeq ($(SINGLEPON),1)
SINGLEP = -DSINGLEP
OPT2 += $(SINGLEP)
endif
#

# To activate long integers set to 1. Else 0
LONGINTON = 1
#
ifeq ($(LONGINTON),1)
LONG = -DLONGINT
OPT2 += $(LONG)
endif
#

# Flags only for Mac...
MACONLYON = 0
ifeq ($(MACONLYON),1)
OPT2 += -DMACONLY
endif

#B ADDONS
#
#B Do not use any of these flags.
#   Are intended for developing and debuging purposes.
#
# To print some useful debuggin info set to 1. Else 0
#   acts also as (thoroughly )DIAGNOSTICS flag...
#   used in some engines, like search_octree_ggg_omp
DEBUGON = 0
DEBUGCOMPILINGON = 0
DEBUGTREEON = 0
DEBUGTRACKINGON = 0
#
#E
#
ifeq ($(DEBUGCOMPILINGON),1)
PROJECT_WARNING_FLAGS = -Wall -Wextra -Wpedantic -Wformat=2 \
    -Werror=format -Werror=implicit-function-declaration -Werror=return-type \
    -Wno-unused-function -Wno-unused-parameter -Wno-unused-variable \
    -Wno-unused-but-set-variable -Wno-format-truncation
endif
ifeq ($(DEBUGON),1)
OPT2 += -DDEBUG
endif
ifeq ($(DEBUGTREEON),1)
OPT2 += -DDEBUGTREE
endif
ifeq ($(DEBUGTRACKINGON),1)
OPT2 += -DDEBUGTRACKING
endif
#
ifeq ($(ADDONSON),1)
OPT2 += -DADDONS
# Machine definitions
include $(MACHINES_DIR)/addons/Makefile_addons
#
else
OPT2 += -DTPCF
endif

#E ADDONS

#
#B GENERALLIBS section
# Use 0 to get rid off all calls to general_libs routines:
# (always set to 1...)
GENERALLIBS_ON = 1

ifeq ($(GENERALLIBS_ON),1)
GENERALLIBS = general_libs
vpath %.c $(MDIR)/$(GENERALLIBS)
INCLUDES += -I$(MDIR)/$(GENERALLIBS)
HEADERFILES += $(wildcard ./general_libs/*.h)
EXTERNAL += clib.o mathfns.o inout.o mathutil.o numrec.o
endif
#
#E

#
#B GETPARAM section
# Use 0  to get rid off all calls to getparam routines:
# (always set to 1... 0 is experimental so far)
GETPARAM_ON = 1

ifeq ($(GETPARAM_ON),1)
GETPARAM = getparam
vpath %.c $(MDIR)/$(GETPARAM)
OPT2 += -DGETPARAM
INCLUDES += -I$(MDIR)/$(GETPARAM)
HEADERFILES += $(wildcard ../general_libs/*.h)
EXTERNAL += getparam.o
endif
#
#E


#---------------------------------------------
# BEGIN GSL and CFITSIO DEFINITIONS (if needed)
#

# -------------------
#B CFITSIO
# It can be necessary to export path to pkgconfig
#export PKG_CONFIG_PATH=/Users/mar/NagBody_pkg/local/cfitsio/lib/pkgconfig:$PKG_CONFIG_PATH

# CFITSIO section
ifeq ($(CFITSIOON),1)

CFITSIO_INCL =
CFITSIO_LDFLAGS =
CFITSIO_LIBS =
CFITSIO_RPATH =

ifeq ($(CFITSIOLIBON),0)

comma := ,

PKG_CONFIG ?= pkg-config
CFITSIO_PKG ?= cfitsio

# Manual override still allowed:
#   CFITSIO_INCLUDE=/path/include CFITSIO_LIB=/path/lib make all
ifneq ($(CFITSIO_INCLUDE),)
    CFITSIO_INCL = -I$(CFITSIO_INCLUDE)
else
    CFITSIO_INCL := $(shell $(PKG_CONFIG) --cflags $(CFITSIO_PKG) 2>/dev/null)
endif

ifneq ($(CFITSIO_LIB),)
    CFITSIO_LDFLAGS = -L$(CFITSIO_LIB)
    CFITSIO_LIBS = -lcfitsio
    CFITSIO_RPATH = -Wl,-rpath,$(CFITSIO_LIB)
else
    CFITSIO_LDFLAGS := $(shell $(PKG_CONFIG) --libs-only-L $(CFITSIO_PKG) 2>/dev/null)
    CFITSIO_LIBS := $(shell $(PKG_CONFIG) --libs-only-l $(CFITSIO_PKG) 2>/dev/null)
    CFITSIO_RPATH = $(patsubst -L%,-Wl$(comma)-rpath$(comma)%,$(CFITSIO_LDFLAGS))
endif

CCFLAG += $(CFITSIO_INCL)
LDFLAG += $(CFITSIO_LDFLAGS) $(CFITSIO_RPATH)
FITSIOLIBS += $(CFITSIO_LIBS)

endif # CFITSIOLIBON=0

endif # CFITSIOON=1

#E new CFITSIO
# -------------------

# ---------------------------------------------
#B GSL DEFINITIONS
##
## Definition to set where GSL are located
##
## Experiment using this lib... apparently is for interfacing with C++
##   therefore look for something similar for C.
##    -L/usr/local/lib/ -lboost_python312
##
#
#
ifeq ($(USEGSL),1)

OPT2 += -DUSEGSL
ifeq ($(GSLINTERNAL),0)

GSL_CONFIG ?= gsl-config

# Allow user override:
#   GSL_INCLUDE=/path/include GSL_LIB=/path/lib make all
ifneq ($(GSL_INCLUDE),)
    GSL_CFLAGS = -I$(GSL_INCLUDE)
else
    GSL_CFLAGS := $(shell $(GSL_CONFIG) --cflags 2>/dev/null)
endif

ifneq ($(GSL_LIB),)
    GSL_LDFLAGS = -L$(GSL_LIB)
    GSL_LIBS = -lgsl -lgslcblas
else
    GSL_LDFLAGS =
    GSL_LIBS := $(shell $(GSL_CONFIG) --libs 2>/dev/null)
endif

CCFLAG += $(GSL_CFLAGS)
LDFLAG += $(GSL_LDFLAGS)
MLIBS += $(GSL_LIBS)

    CCFLAG += -DNOINTERNALGSL

endif # ! GSLINTERNAL

ifeq ($(GSLINTERNAL),1)
include $(MACHINES_DIR)/addons/Makefile_gsl
endif # ! GSLINTERNAL

endif # ! USEGSL
#endif
# ---------------------------------------------
#E GSL DEFINITIONS

#
# END GSL and CFITSIO DEFINITIONS (if needed)
#--------------------------------------------

# Capture the final Make settings in the executable.  Keeping this metadata in
# a generated header preserves strings containing spaces (compiler flags,
# include paths, and linker flags) without fragile command-line quoting.
CBALLS_MAKE_INFO_HEADER := $(WRKDIR)/cballs_make_info.h
CBALLS_MAKE_INFO_GENERATOR := $(MDIR)/scripts/generate_make_info_header.sh
INCLUDES += -I$(WRKDIR)

CBALLS_MAKE_INFO_VARIABLES := \
	DEFDIMENSION USEGSL GSLINTERNAL OPENMPMACHINE ADDONSON \
	SINGLEPON LONGINTON MACONLYON DEBUGON DEBUGCOMPILINGON DEBUGTREEON \
	DEBUGTRACKINGON GENERALLIBS_ON GETPARAM_ON \
	KDTREEOMPON KDTREEMPION KDTREE2BALLSOMPON KDTREE2BALLSMPION \
	BALLTREEOMPON BALLTREEMPION BALLTREE2BALLSOMPON BALLTREE2BALLSMPION \
	BALLTREE2BALLSOMP3PCFON BALLTREE2BALLSMPI3PCFON \
	OCTREE2BALLSOMPON OCTREE2BALLSMPION OCTREEGGGON OCTREEGGGOMPON \
	OCTREEGGGMPION OCTREEGGGCROSSOMPON OCTREEGGGOMPTRIANGLESON \
	OCTREEKKKOMPON OCTREEKKKBALLS4OMPTRIANGLESON \
	OCTREEBALLS4OMPON OCTREEBALLS4MPION OCTREESHEAROMPON \
	OCTREESHEARSPHEREOMPON OCTREESINCOSOMPON OCTREESMOOTHINGON \
	OCTREEBOXOMPON KDTREECUTEBOXON TREEOMPSINCOSON \
	DIRECTMETHODON DIRECTMETHODSIMPLEON DIRECTMETHODSIMPLELOOPIDON \
	BALLSON BALLS0357ON ALLOW_BALLS_WITH_SMOOTHING COMPLEXLIBON \
	COSMOLIBON SAVERESTOREON \
	GGG_OMP_PIVOT_CHUNK_SIZE GGG_OMP_FRONTIER_TARGET_TASKS \
	GGG_OMP_FRONTIER_MIN_ACTIVE GGG_MPI_PIVOT_CLAIM_SIZE \
	LYAFORESTOMPON LYAFORESTMPION LYA1D_OMP_PIVOT_BLOCK_SIZE LYA1D_TREE3_LEAF_SIZE \
	OCTREESHEARSPHERE2BALLSOMPON OCTREESHEARSPHERE2BALLSMPION \
	KDTREESHEARSPHERE2BALLSOMPON KDTREESHEARSPHERE2BALLSMPION \
	BALLTREESHEARSPHERE2BALLSOMPON BALLTREESHEARSPHERE2BALLSMPION \
	SHEAR_OMP_PIVOT_BLOCK_SIZE KDTREEBOXOMPON \
	NEIGHBORBOXESOMPON GADGETIOON CLASSLIBON PXDON IOLIBON \
	CFITSIOLIBON CFITSIOON SMOOTHPIVOTON NOWKAvgON NMultipolesON \
	NONORMHISTON POLARAXISON NOLIMBERON OVERCOUNTINGON \
	NOSTANDARNORMHISTON CMDLINE_DEFS_UNITSPHERE_ON TWOPCFON TPCFON \
	TPCFSHEARON PRUNEON BALLS4SCANLEVON THETA NORMALHISTSCALEON \
	_DEBUGON_ LOGBINON ORIGINALCBON ADDPIVOTNEIGHBOURSON \
	OCTREE3PCF3DOMPON OCTREE3PCF3DMPION CB3D_OMP_PIVOT_BLOCK_SIZE \
	MDIR WRKDIR MACHINES_DIR CC PYTHON OPTFLAG OMPFLAG OPT1 OPT2 OPT2LIB CCFLAG \
	LDFLAG INCLUDES MLIBS FITSIOLIBS EXTERNAL AR SANITIZER \
	MACOSX_DEPLOYMENT_TARGET PKG_CONFIG CFITSIO_PKG CFITSIO_INCLUDE \
	CFITSIO_LIB CFITSIO_INCL CFITSIO_LDFLAGS CFITSIO_LIBS CFITSIO_RPATH \
	GSL_CONFIG GSL_INCLUDE GSL_LIB GSL_CFLAGS GSL_LDFLAGS GSL_LIBS \
	SLEEFON SLEEF_PKG SLEEF_AVAILABLE SLEEF_ENABLED SLEEF_CFLAGS \
	SLEEF_LDFLAGS SLEEF_LIBS SLEEF_RPATH

CBALLS_MAKE_INFO_VARIABLES += BUILD_ADDONS BUILD_CLASSLIB BUILD_OPENMP \
	BUILD_MPI BUILD_LYA BUILD_SHEAR BUILD_PRECISION DEFINEFLAGS \
	SANITIZER_FLAGS PROJECT_WARNING_FLAGS DIMCODE OMPCODE SINGLEP LONG \
	PYTHON_MACOSX_DEPLOYMENT_TARGET OPENMP_MACOSX_DEPLOYMENT_TARGET \
	MACOS_DEPLOYMENT_FLAG GENERALLIBS GETPARAM MPICC UNAME_S

CBALLS_MAKE_INFO_VARIABLES += NATIVE_PAIR_VECTOR_LOG

export $(CBALLS_MAKE_INFO_VARIABLES)

.PHONY: cballs-make-info-force
cballs-make-info-force:

$(CBALLS_MAKE_INFO_HEADER): cballs-make-info-force $(CBALLS_MAKE_INFO_GENERATOR) scripts/build_support.py | .base
	@CBALLS_MAKE_INFO_VARIABLES='$(CBALLS_MAKE_INFO_VARIABLES)' \
		sh $(CBALLS_MAKE_INFO_GENERATOR) $@

CCFLAG += $(OPT2)

VENDORED_SOURCE_PATTERNS = $(if $(strip $(GSL)),$(GSL)/%) \
    $(if $(strip $(CFITSIO_SOURCE_DIR)),$(CFITSIO_SOURCE_DIR)/%)
SOURCE_WARNING_FLAGS = $(if $(filter $(VENDORED_SOURCE_PATTERNS),$<),-w,$(PROJECT_WARNING_FLAGS))

# Compiler-generated dependencies include shared headers and included .c files.
# Publish only successful compilations; a failed build cannot leave a fresh .o.
%.o: %.c | .base
	cd $(WRKDIR) && $(CC) $(OPTFLAG) $(OMPFLAG) $(CCFLAG) $(SOURCE_WARNING_FLAGS) $(INCLUDES) \
		-MMD -MP -MF $*.d.tmp -MT '$*.o' -c $< -o $*.o.tmp \
		&& mv $*.d.tmp $*.d && mv $*.o.tmp $*.o
